- schema_version: 1
- batch_id: manual-2608272335
- batch_base: 9393dbf54901
- verified_commit: 9393dbf54901
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: [{"case":"lciod-liveness","status":"pass","items":["4/4 pass"]},{"case":"lciod-pipeline","status":"pass","items":["stats 校验 enabled==1 生效 pass"]},{"case":"lciod-trigger","status":"pass","items":["6/6 pass 无回归"]}]
- elapsed_s: 600
- summary: lciod 防护栏缺口补齐：enabled==1 假绿修复 + AIDL 投影抽纯函数单测（21字段直传/管理字段省略）；harness 188 全绿、板上单测 33/33、上板三用例复验全 pass

## body

# lciod 防护栏缺口补齐 验证收据正文

## 背景

首轮验证 PASS 后二次盘点（用户确认"防护网是否加全"），发现 lciod 2 处真实遗漏并补齐；lcview 经复盘无对应缺口（lcview pipeline 为端到端，AIDL 层已被隐式覆盖）。

## 改动清单

### 缺口①（lciod_check.py enabled 假绿）
- cases/lciod_check.py：stats 校验加 `enabled == 1` 断言（enabled=0 监控被禁用须判红，
  对齐 lcview logfield overrun/dropped/readErr 故障可见性语义）
- tests/test_lciod_check.py：+2 用例（enabled=0 判红 / enabled 缺失判红）

### 缺口②（AIDL 投影无端到端断言，与 lcview 不对称）
- daemon/service.h：+3 投影纯函数声明（ProjectSystemIoStats/IoConfig/IoEvent）
- daemon/service.cpp：getIoStats/getIoConfig/readIoEvent 投影逻辑抽为纯函数并改调用
  （行为零变更）；省略 currentRate/enabled/flags 管理字段
- tests/Service_test.cpp：+4 用例（21 字段逐一直传 / 源全零脏值覆盖 / config 投影 /
  event 1:1 直传），防字段串位/漏投影回归
- docs/superpowers/specs/2026-08-27-lciod-guardrails-design.md：增补缺口审计章节 6/7

## 验证过程

1. harness pytest：188 passed（含缺口① 2 新例 + 投影 4 新例）
2. 编译（aosp_rpi5-bp1a-userdebug）：lechao_lciod_unit_test + lechao_lciod 全过
3. 板上单测：lechao_lciod_unit_test 33/33 PASSED（原 29 + 投影 4）
4. 上板复验（daemon 二进制已更新重启）：
   - lciod-liveness 4/4 pass（监控日志证明 AIDL 链路持续工作）
   - lciod-pipeline 1/1 pass（enabled==1 断言生效，当前板 enabled=1 实测绿）
   - lciod-trigger 6/6 pass（传输计数 delta 无回归）

## 结论

lciod 防护栏缺口①/② 已补齐，lcview 无对应缺口；两模块防护栏已全（除第 7 节判可接受的
结构性缺口：内核零单测 / main 组装代码 / rc-VINTF 静态校验 / 工具单测 / 事件归因）。
