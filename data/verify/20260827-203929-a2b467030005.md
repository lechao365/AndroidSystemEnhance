- schema_version: 1
- batch_id: a2b467030005
- batch_base: abc006853980
- verified_commit: abc006853980
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass liveness=8/8 pipeline=3/3 warn=1/1 trigger=8/8 transfer=5/5(事件4/5覆盖)
- elapsed_s: 1800
- summary: 补事件 4/5 DT 并加收据删除护栏（transfer 5/5、事件7实测无打点登记暂无手段）

## body

## CDP 原文
-sv base:abc006853980
意图: 补事件 4 与 5 与 7 的 DT 并加收据删除护栏
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) 事件 4 与 5 零覆盖:对 /dev/block 下 U 盘块设备 dd 读写数 MB 产生真实传输,照 trigger 写法加 delta 断言 --event 4 与 --event 5。2) 事件 7 零覆盖:用 /dev/bus/usb 对应节点的 USBDEVFS_RESET 触发复位,加 delta 断言 --event 7。3) 事件 6 与 10 到 13 是错误路径,打点在 LcIod 侧且需 BOT 协议级注入,无 RPi5 单机手段;在 verify-cases 显式登记为暂无触发手段,不计入已覆盖。4) 批次 49 误删收据 20260824-074627 靠 abc0068 补回,data/verify 无删除护栏;加单测断言收据集合只增不删。

## 编辑（方向 1-4）
- 方向 1: 新增 verify-cases 用例 lcview-transfer（baseline → adb shell dd 读 4MB → delta --event 4 → adb shell dd 写 1MB 到分区 → delta --event 5）；新工具 tools/usbreset.c + Android.bp（USBDEVFS_RESET ioctl，编入 /system/bin）
- 方向 2: usbreset 实测（reset ok + daemon 收 201 valid 新事件）但**无 id 7 打点**——usb_reset 打点在 USBD 设备栈路径（vendor_lechao_usbd-stats.c），xhci host 模式不经过（副作用 rate_degraded 出现）；事件 7 归入暂无触发手段
- 方向 3: verify-cases 显式登记事件覆盖盘点（已覆盖 4/5/8/9；暂无手段 6/7/10/11/12/13；预留 1/2/3）
- 方向 4: 新建 tests/test_receipts_guard.py——git 全历史 diff-filter=D 的 data/verify 收据当前必须存在（净删除即红），批次 49 误删场景护栏生效
- 自愈: lcview-transfer 首版 dd/usbreset 为 hostcmd（host 侧执行 /dev/block 与 usbreset 静默失败）→ 改 adb shell 包装（设备侧执行）；写操作落分区（sda1）防破坏分区表

## 单测
- harness 169 全过（+2 收据护栏）

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-liveness → pass 8/8
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → pass 1/1
### lcview-trigger → pass 8/8
### lcview-transfer → **pass 5/5**（事件 4/5 真实传输触发 + delta 断言命中；dd 读 4MB 24M/s、写 1MB 分区、事件 ts>基线）

## 真值
- endpoint: 192.168.1.28:5555
- U 盘块设备: /dev/block/sda（8,0）+ sda1 分区（USB Flash Drive，xhci-host）
- usb 节点: /dev/bus/usb/001/002
- usbreset: /system/bin/usbreset 部署 + 实测 reset ok
- 事件 7 结论: USBDEVFS_RESET 复位成功但 xhci host 路径无 usb_reset 打点（USBD 设备栈专属），登记暂无手段
