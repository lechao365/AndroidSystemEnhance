- schema_version: 1
- batch_id: ae65531af480
- batch_base: e30587d4c513
- verified_commit: e30587d4c513
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass pipeline=3/3 warn=1/1 trigger=8/8(含fresh/ts) unit=73/73 hal=10/10
- elapsed_s: 4800
- summary: 修 hal_test 全灭与三红与时钟（hal 0/10→10/10、unit 71/74→73/73、四用例全绿）

## body

## CDP 原文
-sv base:e30587d4c513
意图: 修 hal_test 全灭与三红与时钟
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) LcView_test.cpp makeLcView 用 new 绕过 SharedRefBase,析构断言 abort 致 0/10;改 ndk::SharedRefBase::make<LcView> 返 shared_ptr 并同步调用方。2) SchemaParser_test.cpp 三红因类型不匹配先命中,EOF 与 exceeds 不可达;改记录首字段为 string,UnknownWireType 因 default 不可达删。3) ws_acceptance.py L255 clock_sync 被 wait_ready 门禁,无 reboot 不跑,skew 28723s 判红;改含 ts 或 fresh 也跑。4) verify-cases.yaml fresh 与 ts 无新事件永不能过,从 pipeline 与 warn 移入 trigger probe 后。

## 编辑（方向 1-4，code/rpi5 + harness）
- 方向 1: LcView_test.cpp makeLcView 改 ndk::SharedRefBase::make<LcView> 返 shared_ptr；3 处栈对象同步改 make（析构断言 abort 消除）；EXPECT_DEATH 块加 300ms 等待（线程竞态致假"failed to die"）；open 重试间隔 1s，重试断言等待 200ms→1500ms
- 方向 2: SchemaParser_test.cpp kValidJson 首字段改 string + buildValidRecord 同步（STRING 首、INT64 次）；TypeMismatch 改 wire=INT64；StringFieldLenEof/DataExceeds field_count 保持 2（设 1 先命中 field count mismatch）；BigEndian len_offset 修正为 hdr+1；删 UnknownWireType（default 分支不可达）
- 方向 3: ws_acceptance.py need_clock = --wait-ready 或验收含 --mode fresh/ts（时间敏感判据自动校准）
- 方向 4: verify-cases.yaml fresh/ts 从 pipeline/warn 移入 trigger probe 之后（事件驱动无新事件永不能过）
- 自愈（verify 阶段联动缺陷）: 时钟校准回拨后历史记录 ts 污染 baseline max_ts 致 delta 恒 0——lcview_check.py 新增 _trusted_tss 排除超过设备时钟 300s 的未来 ts（baseline/delta/ts 三处）+ 单测

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ overall=pass 3/3
- svc:lechao_lcview_hal: pass（init.svc running）
- svc:lechao_lcview: pass（init.svc running）
- boot: pass（sys.boot_completed=1）

### lcview-pipeline → pass 3/3（fresh 已移出）
files / valid_json / schema 全过

### lcview-pipeline-warn → pass 1/1（ts 已移出）
invalid（invalid_records.log 为空）

### lcview-trigger → pass 8/8（首次含 fresh/ts 全链路）
- adb root（显式）: pass
- baseline: pass（max_ts=校准后可信值）
- authorize_off（echo 0）: pass
- disconnect 增量（--event 9）: pass
- authorize_on（echo 1）: pass
- probe 增量（--event 8 --vid 1256 --pid 25344）: pass
- fresh --window 600: pass（probe 新事件后最新写入秒级）
- ts --skew 600: pass（新记录 ts 与设备时钟偏差在窗内）

## 单测（方向 1/2 修复后全绿）
- lechao_lcview_unit_test: 73/73 PASSED（原 71/74，删 UnknownWireType 不可达用例后 73 个全过）
- lechao_lcview_hal_test: 10/10 PASSED（原 0/10 abort 全灭 → SharedRefBase make 修复后全绿）

## 真值
- endpoint: 192.168.1.28:5555
- 时钟校准: 方向 3 生效——trigger 前自动 clock_sync，校准后 skew=1s（原 -28723s）
- USB: DEV=1-2 VID=1256(04e8) PID=25344(6300)
