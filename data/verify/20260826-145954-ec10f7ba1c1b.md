- schema_version: 1
- batch_id: ec10f7ba1c1b
- batch_base: ce665f1d310e
- verified_commit: ce665f1d310e
- verify_mode: none
- result: skip
- build: skip
- push_board: skip
- acceptance: 
- elapsed_s: 0
- summary: 测试源码归档进 code（-s 无需上板）

## body

# 批次执行报告（batch_id: ec10f7ba1c1b，-s 模式）

## CDP 原文
-s base:ce665f1d310e
意图: 测试源码归档进 code 消除不可复现,补 --case 一致性,量化扫描盲区
验收: 无
方向: 1) workspace 的 lcview tests 目录归档到 code/rpi5/aosp/new 下同构路径,含 Android.bp 与 4 个 _test.cpp 共 5 文件。2) 跑 sync_workspace_to_code.py --gen-manifest-only 重生成 manifest。3) workspace-verify SKILL L70 步骤 5 命令由 --acceptance 改 --case,与 L19 与 L26 一等入口一致。4) 收据报告 vendor/lechao 全域含 lciod 相对 code/aosp/new 的未归档文件清单。5) 对照 .repo/project.list 说明 vendor/lechao 为何两条 _scan_extra_aosp 路径均不覆盖。6) 该盲区记入 sync-code-to-workspace SKILL 已知限制。

## 方向逐条落地
1. workspace lcview tests 5 文件归档到 code/rpi5/aosp/new/vendor/lechao/services/lechao_lcview/tests/ ✓
   （Android.bp + FileWriter_test.cpp + LcView_test.cpp + SchemaParser_test.cpp + record_codec_test.cpp，diff -r 与 workspace 完全一致）
2. sync_workspace_to_code.py --gen-manifest-only 重生成 manifest ✓（新增 5 条 tests 映射）
3. workspace-verify SKILL 步骤 5 命令改 --case（一等入口默认 lcview-liveness，--acceptance 降兜底），与 L19/L26 一致 ✓
4. vendor/lechao 全域未归档清单（见下节）✓
5. 盲区根因说明（见下节，对照 .repo/project.list）✓
6. 盲区记入 sync-code-to-workspace SKILL「已知限制」小节 ✓

## vendor/lechao 全域未归档文件清单（方向 4，相对 code/rpi5/aosp/new/vendor/lechao）
对比 workspace ~/workspace/aosp/vendor/lechao 与 code 同构路径，排除 .git 内部：
- lciod 部分：无未归档（全量已归档）
- lcview 部分本批已归档 tests/ 5 文件后，剩余未归档：
  1. services/lechao_lcview/aidl_api/vendor.lechao.lcview/1/.hash
  2. services/lechao_lcview/aidl_api/vendor.lechao.lcview/1/vendor/lechao/lcview/ILcView.aidl
     （AIDL 版本目录 1/ 未归档；code 仅含 current/ 版本，属遗留待归档项）
- vendor/lechao/.gitignore（项目自身忽略规则，非源码，不归档）

## 盲区根因（方向 5，对照 .repo/project.list）
.repo/project.list 共 985 项，vendor 下仅有 `vendor/brcm`；`vendor/lechao` 未登记（独立 git 项目非 repo 管理）。
两条 _scan_extra_aosp 路径均不覆盖：
1. _scan_extra_aosp（L421）：仅遍历 project.list 内项目做 git status --porcelain，
   vendor/lechao 不在列表 → 永不进入扫描
2. _scan_extra_aosp_non_repo（L466）：top_projects = {p.split("/",1)[0]} 因 vendor/brcm
   而含 "vendor"，L488 `if bn in top_projects: continue` 直接跳过整个 workspace/vendor
   顶层目录；is_excluded_dir("vendor") 为 False（排除正则仅 out|prebuilts|.git|__pycache__），
   即非排除正则所致，而是 project.list 存在 vendor/brcm 使 vendor 进入 top_projects
3. 后果：vendor/lechao 未归档文件（tests/、aidl_api/1/）不被 EXTRA 扫描发现，只能人工归档
