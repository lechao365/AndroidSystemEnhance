- schema_version: 1
- batch_id: d1dfcb778909
- batch_base: e44ccde31d62
- verified_commit: e44ccde31d62
- verify_mode: none
- result: skip
- build: skip
- push_board: skip
- acceptance: 
- elapsed_s: 0
- summary: 冻结版 AIDL 归档 + Windows 串口转发器（-s 无需上板）

## body

# 批次执行报告（batch_id: d1dfcb778909，-s 模式）

## CDP 原文
-s base:e44ccde31d62
意图: 冻结版 AIDL 归档补齐可复现,落地 Windows 侧串口转发器
验收: 无
方向: 1) workspace 的 aidl_api/vendor.lechao.lcview/1/ 归档到 code/rpi5/aosp/new 同构路径,含 .hash 与 ILcView.aidl,code 仅有 current,缺冻结版则 VINTF 基准不可复现。2) 跑 --gen-manifest-only 重生成 manifest。3) 新建 harness/skills/workspace-verify/serial_bridge.py,Windows 侧跑,pyserial 开串口与 TCP 监听双向互转,默认 COM5 115200 与 0.0.0.0:9700。4) 串口读线程广播给全部 TCP 客户端,客户端写串口加锁串行化。5) 缺 pyserial 返 3,串口打不开或端口占用返 1。6) 部署说明写明目录互访与网络可达是两回事,NAT 需宿主 IP,mirrored 下 localhost 直通。

## 方向逐条落地
1. aidl_api/vendor.lechao.lcview/1/ 冻结版归档到 code/rpi5/aosp/new 同构路径 ✓
   （.hash=487c066f0899989b2aaced7428920f8b42947f90 + vendor/lechao/lcview/ILcView.aidl，
   diff -r 与 workspace 一致；VINTF 基准冻结版补齐可复现）
2. --gen-manifest-only 重生成 manifest ✓（新增 2 条冻结版映射）
3. 新建 harness/skills/workspace-verify/serial_bridge.py ✓
   （Windows 侧，pyserial 开串口 + TCP 监听双向互转，默认 COM5 115200 与 0.0.0.0:9700）
4. 串口读线程广播给全部 TCP 客户端 + 客户端写串口加锁串行化 ✓（ser_lock 单写者，与 WSL 侧 flock 对称）
5. 缺 pyserial 返 3，串口打不开或端口占用返 1 ✓（均实测）
6. 部署说明 ✓（docstring 写明目录互访≠网络可达；NAT 需宿主 IP 且监听 0.0.0.0；
   mirrored 下 localhost 直通）

## 验证结果（Linux 侧实测，pty 模拟串口）
- py_compile 语法通过
- 缺 pyserial：模拟 ImportError → 返 3 ✓
- 端口占用（9700 被占 + 伪串口）→ 返 1 ✓
- 正常启动 + 双向转发：客户端→串口写字节、串口→客户端读广播，字节逐一比对通过 ✓
