- schema_version: 1
- batch_id: a934b5b6da4a
- batch_base: 87664a1b17ae
- verified_commit: 87664a1b17ae
- verify_mode: none
- result: skip
- build: skip
- push_board: skip
- acceptance: 
- elapsed_s: 0
- summary: WSL 侧串口客户端落地（-s 无需上板）

## body

# 批次执行报告（batch_id: a934b5b6da4a，-s 模式）

## CDP 原文
-s base:87664a1b17ae
意图: WSL 侧串口客户端落地,消掉转发器黑洞误判
验收: 无
方向: 1) 新建 harness/skills/workspace-verify/ws_serial.py,WSL 侧纯标准库 socket,不用 pyserial 与 fcntl。2) SerialConn 含 connect,send_line 末尾发 CR,drain 发命令前弃残留,read_until_marker。3) exec 下发 cmd 后接 echo __LE_EXIT_CODE__=$?,复用 ws_adb_connect.parse_exec_output。4) 三错误分类,ENDPOINT_UNREACHABLE 即 TCP 连不上,DEVICE_UNRESPONSIVE 即超时,SERIAL_SILENT 即 recv 返空。5) 子命令 status exec read ip,ip 跑 ip -o -4 addr show wlan0。6) serial_bridge 串口读失败须关监听退出,否则 WSL 见已连接无数据会误判设备全砖。

## 方向逐条落地
1. 新建 harness/skills/workspace-verify/ws_serial.py ✓（纯标准库 socket+argparse+re+os，无 pyserial/fcntl；写互斥由转发器 ser_lock 承担）
2. SerialConn 四方法 ✓（connect TCP 连接 / send_line 末尾发 CR / drain 非阻塞弃残留 / read_until_marker 累积读至命中正则）
3. exec 复用 ws_adb_connect.parse_exec_output ✓（_execute：drain → send_line(cmd) → send_line(echo __LE_EXIT_CODE__=$?) → read_until_marker → parse）
4. 三错误分类 ✓（ENDPOINT_UNREACHABLE=connect 失败；DEVICE_UNRESPONSIVE=读超时/未捕获 marker；SERIAL_SILENT=recv 返空）
5. 子命令 status/exec/read/ip ✓（ip 下发 ip -o -4 addr show wlan0，过滤环回/169.254/0.0.0.0）
6. serial_bridge 串口读失败关监听退出 ✓（_serial_reader 置 shutdown 事件 → main 关 server+断开全部客户端+关串口）

## 验证结果（Linux 侧 pty 伪串口 + serial_bridge + ws_serial 全链路实测）
- status → REACHABLE（rc 0）✓
- exec "echo hello" → device-output: echo hello（rc 0，marker 复用解析含退出码 int 比对修复）✓
- ip → 192.168.1.50（过滤逻辑实测）✓
- read --timeout 1 → rc 0 ✓
- ENDPOINT_UNREACHABLE：bridge 未监听时实测报 TCP 连不上: [Errno 111] Connection refused ✓
- DEVICE_UNRESPONSIVE：设备无响应实测报 读取超时（10.0s 内未捕获 marker）✓
- SERIAL_SILENT：黑洞场景客户端 recv 返 b'' 实测 ✓
- 黑洞退出（方向 6）：关 pty master 模拟串口故障 → bridge 日志「串口读取失败，关闭监听退出」→
  进程退出（poll 0）→ 客户端 recv 立即返空感知断开，不再黑洞误判全砖 ✓

## 测试过程备注
- exec 退出码比对曾因 parse_exec_output 返回 int 而与字符串比较恒假，已修为 str(code) 比对并复测通过
- 验证脚本经 /tmp/opencode/ 独立文件驱动（heredoc 与 pkill -f 同命令会自匹配误杀 bash，已规避）
