- schema_version: 1
- batch_id: manual-2608262314
- batch_base: dev
- verified_commit: dev
- verify_mode: board
- result: pass
- build: skip
- push_board: pass
- acceptance: jsonl_files_exist=pass jsonl_valid_json=pass jsonl_schema_match=pass jsonl_no_invalid=pass usb_disconnect_recorded=pass usb_probe_recorded=pass jsonl_fresh=pass jsonl_ts_reasonable=pass
- elapsed_s: 1200
- summary: lcview 业务板端验证通过（L1 管道健康 4PASS + L2 触发全链路 5/5PASS，触发后数据流恢复）

## body

# lcview 业务板端验证（L1 被动数据管道 + L2 触发型全链路）

## 目标
验证 lcview 业务在开发板（rpi5）上工作 OK——事件采集→传输→解析→落盘全链路业务数据正确。

## 用例资产（harness/skills/workspace-verify/cases/，随本批入库）
- lcview_pipeline.yaml（L1 被动 6 用例）/ lcview_trigger.yaml（L2 触发 5 用例）
- lcview_check.py/.sh（host 侧校验器：adb pull JSONL+schema，host 解析；delta 基线 ts 增量判定）
- 基于 lc-skills-loop-engineering（run_on: host / device）

## L1 被动验证（首轮 4PASS/1FAIL/1WARN，session attempt=1）
- PASS jsonl_files_exist：16 个 jsonl，8 个非空（历史 1512 条）
- PASS jsonl_valid_json：1512 条全合法 JSON，坏行 0
- PASS jsonl_schema_match：id∈{4..13} 且 f 字段数与 schema 全匹配，mismatch 0
- PASS jsonl_no_invalid：invalid_records.log 为空（无坏记录）
- FAIL jsonl_fresh：最新写入 mtime_age=2636s 超窗 600s（服务未在持续写入）
- WARN jsonl_ts_reasonable：最新记录 ts 偏差 5551923s（最新记录停在 6/23）

结论：管道健康，但今日无新事件（事件驱动模型，设备常驻无活动不产生事件，非缺陷）

## L2 触发验证（5/5 全 PASS，session attempt=2）
- PASS lcview_baseline：基线写入（max_ts + 各文件行数）
- PASS usb_authorize_off：echo 0 > /sys/bus/usb/devices/1-2/authorized（device）
- PASS usb_disconnect_recorded：新增 usb_disconnect(9) 记录（f=[0]）
- PASS usb_authorize_on：echo 1 > authorized（device）
- PASS usb_probe_recorded：新增 usb_probe(8) 且 vid/pid 匹配
  f=[0,1256,25344,"Samsung","Flash Drive"]（vid=0x04E8 pid=0x6300 精确匹配）

## L2 触发后重跑 L1（数据流恢复）
- PASS jsonl_fresh：最新写入 mtime_age=18s
- PASS jsonl_ts_reasonable：最新记录 ts 与设备时钟偏差 20.5s
- 触发激活 5 类事件全部落盘：probe/disconnect/transport_start/transport_end/rate_degraded

## 结论
lcview 业务在开发板上工作 OK：
1) 管道健康：历史 1512+ 条记录全合法、schema 全匹配、无坏记录
2) 服务存活：svc×2 running、心跳 readErr=0 overrun=0 dropped=0
3) 事件源活跃：authorized 切换触发 disconnect/probe 增量落盘，字段精确匹配
4) 全链路证据：内核 LcIod hook → ring → HAL → daemon 解析 → JSONL 正确

## 现场证据（loop-engineering artifacts）
- harness/log/loop-lcview/artifacts/evidence.json / summary.txt（gitignore，本地保留）
- 新记录样例：
  {"ts":1787785397204318158,"id":8,"level":1,"f":[0,1256,25344,"Samsung","Flash Drive"]}
  {"ts":1787785391173076106,"id":9,"level":1,"f":[0]}
