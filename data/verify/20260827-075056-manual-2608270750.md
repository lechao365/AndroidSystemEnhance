- schema_version: 1
- batch_id: manual-2608270750
- batch_base: dev
- verified_commit: dev
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc:lechao_lcview_hal=pass svc:lechao_lcview=pass log:"build=LCVIEW-VERIFY-20260826-01"=pass boot=pass jsonl_files_exist=pass jsonl_valid_json=pass jsonl_schema_match=pass jsonl_no_invalid=pass jsonl_fresh=pass jsonl_ts_reasonable=pass usb_disconnect_recorded=pass usb_probe_recorded=pass
- elapsed_s: 1800
- summary: lcview 业务链路日志验证通过（build tag 命中16次+batch parsed 计数证据+L1 6/6+L2 5/5，防假验证达成）

## body

# lcview 业务链路日志验证（新 build tag + batch parsed 日志，防假验证）

## 目标
用户担忧此前上板验证为"假验证"（可能只是服务存活）。本批在关键链路新增唯一
build tag 与批次解析统计日志，上板后以日志更新为铁证验证全流程真实工作。

## 代码改动（code 源头，同步 workspace 编译）
1. daemon lechao_lcview.cpp：
   - 新增 LCVIEW_BUILD_TAG="LCVIEW-VERIFY-20260826-01"，启动日志携带
   - 新增批次解析统计日志："batch parsed: %u valid, %u invalid (build=...)"
     （每批次记录 valid/invalid 计数——业务链路核心证据）
2. hal main_lcview_hal.cpp：启动日志携带同值 build tag
3. 编译验证：strings 确认两个二进制均含 tag（各 1 次命中），
   md5 推送后设备端一致（f76bd4... / 2a539b...）

## 上板验证过程（workspace-verify）
1. adb push 2 二进制（system/bin + vendor/bin）→ reboot（07:47:36）
2. PIT-5 复发：设备时钟回退 06-23 → adb root 后 date 修正 08-27 07:48
3. ctl.restart 重启两个服务
4. 验收 pass：svc×2 running + log:"build=LCVIEW-VERIFY-20260826-01" 命中 + boot

## 核心证据：日志真实更新（非假验证）
build tag 命中 16 次（daemon/HAL 启动日志均带）：
- lechao_lcview: starting, build=LCVIEW-VERIFY-20260826-01
- LcView HAL: starting, loglevel=production, build=LCVIEW-VERIFY-20260826-01

业务链路日志（batch parsed，L2 触发前后）：
- reboot 后：batch parsed: 14 valid, 0 invalid / 437 valid, 0 invalid
- L2 authorized 切换（disconnect）：08-27 07:48:24 batch parsed: 1 valid, 0 invalid
- L2 authorized 切换（probe）：08-27 07:48:30 batch parsed: 1 valid, 0 invalid
- 重连后系统访问：08-27 07:48:31 batch parsed: 504 valid, 0 invalid
- 全部带 build tag，invalid=0（无坏记录）

## 用例套件结果（loop-engineering）
- L1 lcview_pipeline：6/6 PASS（files/valid_json/schema/no_invalid/fresh/ts）
- L2 lcview_trigger：5/5 PASS（baseline→authorize_off→disconnect→authorize_on→probe+vid/pid）
- 新记录落盘：8_usb_probe/9_usb_disconnect 20260827 各 1 条 + transport/rate 事件

## 结论
验证为真：新日志标识上板后确实更新（build tag 唯一、batch parsed 计数随事件
增长、时间戳与设备时钟一致、字段匹配），证明"内核 hook→ring→HAL→daemon→JSONL"
全链路真实工作。
