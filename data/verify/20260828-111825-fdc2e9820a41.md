- schema_version: 1
- batch_id: fdc2e9820a41
- batch_base: 01e77d3e84b5
- verified_commit: 01e77d3e84b5
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: {"overall":"pass","items":[{"tag":"svc:lechao_lcview_hal","status":"pass","detail":"init.svc.lechao_lcview_hal='running' exit=0"},{"tag":"svc:lechao_lcview","status":"pass","detail":"init.svc.lechao_lcview='running' exit=0"},{"tag":"log:\"LcView: alive beat=\"","status":"pass","detail":"logcat 命中 关键字 'LcView: alive beat='（取回 56183 字符）"},{"tag":"log:\"heartbeat, loop=\"","status":"pass","detail":"logcat 命中 关键字 'heartbeat, loop='（取回 56321 字符）"},{"tag":"logfield:\"LcView: alive beat=|overrun|=|0\"","status":"pass","detail":"logfield overrun=0 = 0（锚点末行 beat=90）"},{"tag":"logfield:\"LcView: alive beat=|dropped|=|0\"","status":"pass","detail":"logfield dropped=0 = 0（锚点末行 beat=90）"},{"tag":"logfield:\"LcView: alive beat=|readErr|=|0\"","status":"pass","detail":"logfield readErr=0 = 0（锚点末行 beat=90）"},{"tag":"hostcmd:\"cases/lcview_check.sh --mode conserve\"","status":"pass","detail":"心跳末行: total_records=90 overrun=0 jsonl_records=90 在途差值=0（上限 512）OK 守恒成立"},{"tag":"boot","status":"pass","detail":"sys.boot_completed='1'"}]}
- elapsed_s: 0
- summary: 守恒判据自动化并补 V2 推送与 ring 单测：conserve 模式挂 lcview-liveness、V2-ndk 登记 system/vendor lib64、ring 纯逻辑抽 host 单测 51 断言过，新内核上板 lcview-liveness 9/9 全过守恒成立

## body

# verify 收据 body — batch fdc2e9820a41

## CDP 原文
```
-sv base:01e77d3e84b5
意图: 守恒判据自动化并补 V2 推送与 ring 单测
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) 守恒式只进心跳日志,lcview_check.py 的 MODES 无守恒模式,守恒结论系人工算得;加 conserve 模式取心跳末行三数判决(total_records/overrun/jsonl_records),在途差值超界判红,挂进 lcview-liveness。2) vendor.lechao.lcview-V2-ndk.so 未登记进 verify-cases 的 modules.lcview.push,上批因其未推送致 reboot 后 crash loop 靠手工补推;补登记到 system 与 vendor lib64。3) lcview_ring.c 零单测;把 ring_avail_write 与两个 ring_memcpy 与 ring_evict_one 的索引环绕驱逐逻辑抽 host 单测,内核 API 用 shim 桩掉不引 KUnit。
```

## 方向 1 — conserve 守恒判据自动化
- lcview_check.py：新增 mode_conserve——`adb logcat -d -t 5000` 取 daemon 心跳
  （锚点 `heartbeat, loop=`）末行，正则提取 total_records/overrun/jsonl_records 三数，
  在途差值 = total - overrun - jsonl：<0 判红（重复落盘/计数异常）、
  > --in-flight（默认 512）判红（内核产生未落盘积压）；锚点未命中判红（无心跳
  数据无法判守恒）。注册进 MODES + argparse 增 --in-flight。
- verify-cases.yaml lcview-liveness：追加 `hostcmd:"cases/lcview_check.sh --mode conserve"`（注意5 注释）。
- test_ws_acceptance.py 期望串同步（内建 lcview-liveness 解析断言），40 单测全过。

## 方向 2 — V2-ndk 补登记 modules.lcview.push
- verify-cases.yaml modules.lcview.push 追加：
  `vendor.lechao.lcview-V2-ndk` → /system/lib64 + /vendor/lib64 各一份
  （注释说明：hal/daemon 双依赖，漏推必 crash loop，2026-08-28 实拍）。
- 板上核对：/system/lib64 与 /vendor/lib64 的 vendor.lechao.lcview-V2-ndk.so 均在
  （上批产物），服务 running——登记与板上实际一致。

## 方向 3 — lcview_ring host 单测（抽纯逻辑，不引 KUnit）
- 新增 lcview_ring_logic.h/.c：抽 4 个纯索引算法（ring_avail_write /
  ring_memcpy_out / ring_memcpy_in / ring_evict_one），仅依赖 uint32_t/memcpy，
  内核 API 全部剥离（等价 shim 语义）；default_record_len 由调用方传，
  损坏记录（len==0 或 >size）经返回码 2 通知内核侧打 pr_warn（保持原行为）。
- lcview_ring.c 4 个 static 函数改为薄包装调用 core（行为零变更）。
- Makefile：lcview-objs 追加 lcview_ring_logic.o。
- 新增 tests/lcview_ring_host_test.c + Makefile：51 断言（avail 空/满/环绕边界、
  memcpy 跨尾部 wrap、evict 前缀跨尾部读取/损坏回退/空环/read_pos 环绕），
  host gcc 编译运行，**code/ 与 workspace 双侧均 OK: all 51 checks passed**。

## 单测 / 编译
- harness 侧：pytest test_ws_acceptance.py 40 passed（含 conserve 期望串）。
- host 单测：lcview_ring_host_test 51 checks passed（code 与 workspace 双侧）。
- 内核增量编译：make Image dtbs（AOSP Clang+LLD，android_rpi5_defconfig）成功，
  Image=35M（Linux version 6.6.116-v8-g1b2670a01a81-dirty）。
  排障：首编 lcview_ring_logic.c 报 `<string.h> not found`（内核 freestanding 无
  用户态 stdlib）→ 改 `#ifdef __KERNEL__` 用 `<linux/string.h>`，重编通过。
- AOSP 侧无产品源码改动，模块无需重编；boot.img 增量构建成功。

## 推送与刷写
- 内核产物（Image + bcm2712*.dtb + overlays/*.dtbo 同源）拷贝到
  device/brcm/rpi5-kernel/，make bootimage 成功。
- 人工确认门通过：adb push boot.img → dd 到 /dev/block/mmcblk0p1（32 条 4M=128M）
  → sync → reboot。

## 验收逐项（lcview-liveness → pass 9/9）
- svc:lechao_lcview_hal → pass（running）
- svc:lechao_lcview → pass（running）
- log:"LcView: alive beat=" → pass（HAL 心跳持续，取回 56183 字符）
- log:"heartbeat, loop=" → pass（daemon 心跳含守恒三数）
- logfield overrun=0 / dropped=0 / readErr=0 → pass ×3（锚点末行 beat=90 全 0）
- hostcmd conserve → pass：心跳末行 total_records=90 overrun=0 jsonl_records=90
  在途差值=0（≤512）——新内核下守恒成立，无丢记录/重复
- boot → pass（sys.boot_completed=1）

## 失败现场（首轮验收 log 判据 miss，PIT-5 同类，已修复）
- 首轮 --log-since 用本地 CST 时间 "08-28 11:16:00.000"，而设备时钟为 UTC
  （GMT 03:17，时区差 8h）→ logcat 时间窗落在设备时钟未来，log/logfield 取回
  0 字符判红（conserve 不受影响，用 -t 5000 行数窗仍命中）。
- 修复：--log-since 改用设备 UTC 时刻 "08-28 03:16:00.000" 重跑，9/9 全过。
  设备 epoch 时间本身准确（03:17 UTC = 11:17 CST），非时钟回退，无需 date 校准。
