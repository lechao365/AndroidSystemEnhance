- schema_version: 1
- batch_id: manual-2608272316
- batch_base: 9393dbf54901
- verified_commit: 9393dbf54901
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: [{"case":"lciod-liveness","status":"pass","items":["svc:lechao_lciod_hal=pass","svc:lechao_lciod=pass","log:monitor=pass","boot=pass"]},{"case":"lciod-pipeline","status":"pass","items":["hostcmd:lciod_check stats=pass(1设备,30字段,abi=2)"]},{"case":"lciod-trigger","status":"pass","items":["root=pass","baseline--reset=pass","dd-read=pass","delta read_bytes/read_cmds=pass(delta=4325376/66)","dd-write=pass","delta write_bytes/write_cmds=pass"]},{"case":"lcview-liveness","status":"pass","items":["8/8 pass"]},{"case":"lcview-pipeline","status":"pass","items":["3/3 pass"]},{"case":"lcview-pipeline-warn","status":"pass","items":["2/2 pass"]},{"case":"lcview-transfer","status":"pass","items":["5/5 pass"]},{"case":"lcview-trigger","status":"pass","items":["8/8 pass"]}]
- elapsed_s: 5400
- summary: lciod 防护栏全新构建+lcview轻量收口：4测试target编译过、板上单测166全绿(lciod 29+28/lcview回归91+18)、lciod上板3用例与lcview回归5用例全pass、ABI契约快照当场兑现(248B修正)

## body

# lciod 防护栏构建 + lcview 轻量收口 验证收据正文

## 验证目标

lciod 模块全新防护栏（单测/取数工具/上板用例）+ lcview 轻量收口，全部验证 PASS 后作为 code 质量表征，供基线生成决策。

## 改动清单（target 9393dbf54901 工作树，20 文件）

### lciod 生产源码重构（行为零变更，对齐 lcview main 抽出模式）
- 新增 hal/hal_service.h：DeviceEntry + IoHalImpl 完整类声明（filegroup/单测复用前置）
- hal/hal_service.cpp：main 移出、类外定义（9 方法）
- 新增 hal/main_lciod_hal.cpp：HAL 进程入口
- 新增 daemon/service.h：IoServiceImpl 类声明 + ComputeAverageRate/ComputeKbRate 纯函数声明
- daemon/service.cpp：main 移出、类外定义、除零公式收敛纯函数
- 新增 daemon/main_lciod.cpp：daemon 进程入口
- daemon/hal_client.h/.cpp：RetryIntervalMs 静态纯函数抽取（负数 clamp 防移位 UB）
- daemon/Android.bp + hal/Android.bp：filegroup（lechao_lciod_daemon_sources / lechao_lciod_hal_sources）

### lciod 防护栏新增
- tests/Android.bp + 5 测试文件（MinorUtils/DeviceIo/HalService/Service/HalClient）：
  2 个 cc_test（lechao_lciod_unit_test general-tests / lechao_lciod_hal_test device-tests，
  native_coverage: true），含 ABI 契约快照（stats=248/event=24/config=8、ABI v2）
- tools/lciod_probe.c + tools/Android.bp：内核 GET_STATS 取数工具（/system/bin，--reset 支持）
- harness/config/verify-cases.yaml：lciod-liveness / lciod-pipeline / lciod-trigger 三用例
  + modules.lciod 登记（targets/test_targets/push 映射）+ lcview 事件登记 LcIod 维度补充
- 新增 cases/lciod_check.sh + lciod_check.py（stats/baseline/delta 三模式防假绿校验器）
- 新增 tests/test_lciod_check.py（24 用例纯函数级）
- AGENTS.md：测试门禁扩展四 target
- code/rpi5/manifest.yaml：lciod tests/tools/main 头文件条目追加（+10 条）

### lcview 轻量收口
- 生产源码零改动；verify-cases.yaml 事件登记注释区补 LcIod stats 维度交叉引用

## 验证过程（sync 22:58 起）

1. 同步：sync_code_to_workspace --auto，18 条 applied，residual=0，校验通过
2. 编译（aosp_rpi5-bp1a-userdebug）：
   - 测试 targets：lechao_lcview_unit_test / lechao_lcview_hal_test /
     lechao_lciod_unit_test / lechao_lciod_hal_test —— pass
   - 部署 targets：lechao_lciod / lechao_lciod_hal / lciod_probe /
     vendor.lechao.lciod.IIoHal-vintf —— pass
3. 推送：push 映射 6 产物（/system ×3 + /vendor ×3）+ remount + 服务重启 OK
4. 板上单元测试（push nativetest64 执行）：
   - lechao_lciod_unit_test：29/29 PASSED
   - lechao_lciod_hal_test：28/28 PASSED（首跑 27/28，ABI 快照判红暴露 stats
     实际 248B vs 预期 240B 的 4 字节尾部 padding 漏算——修正预期后全绿，
     契约快照护栏当场兑现价值）
   - lechao_lcview_unit_test 回归：91/91 PASSED
   - lechao_lcview_hal_test 回归：18/18 PASSED
5. harness pytest：186 passed（含 test_lciod_check 24 例）

## 验收结果（ws_acceptance --case 逐条，全 pass）

- lciod-liveness：svc×2 + log:"monitor: minor=" + boot = pass
- lciod-pipeline：probe stats 校验（1 设备、30 字段齐全、abi=2、vendor/product 非空）= pass
- lciod-trigger：root → baseline --reset → dd 4MB 读 → delta read_bytes/read_cmds
  严格增（delta=4325376/66）→ dd 1MB 写 → delta write_bytes/write_cmds 严格增 = 6/6 pass
- lcview-liveness 8/8 / lcview-pipeline 3/3 / lcview-pipeline-warn 2/2 /
  lcview-transfer 5/5 / lcview-trigger 8/8 —— 回归全 pass

## 过程中发现并修复的问题（防护栏/自愈记录）

1. HalService_test 首编失败：SharedRefBase 私有 operator new，new IoHalImpl() 编译错
   → 改 SharedRefBase::make（NDK binder 契约）
2. lciod_check.py main() stats 分支 UnboundLocalError（report 未初始化）→ 初始化修复
3. lciod_check.py diff_devices 基线结构不一致（devices 包裹层 vs 直接 minor 键）
   → 统一为直接格式，trigger delta 由 fail 转 pass
4. lciod_check.py load_baseline 对 vendor/product 文本字段误做 int 转换 → 仅数值字段转换
5. ABI 快照 240 → 248 修正（同上）
