- schema_version: 1
- batch_id: 852d5d24314c
- batch_base: 90676d388677
- verified_commit: 90676d388677
- verify_mode: board
- result: pass
- build: skip
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass pipeline=3/3 warn=1/1 trigger=8/8
- elapsed_s: 900
- summary: 修 trusted_tss 两处假绿与 manifest 护栏（四用例全绿）

## body

## CDP 原文
-sv base:90676d388677
意图: 修 trusted_tss 两处假绿与 manifest 护栏
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) lcview_check.py L320 max(trusted,default=0) 绕过 L309 护栏:trusted 被滤空时 max_ts 落 0,delta 把历史全当新增假绿;trusted 空即拒写基线返 1,照 mode_ts L273。2) mode_ts 未来向被自身过滤架空:滤阈 300 硬编码小于 skew 默认 600,超 300s 的被丢弃余下恒过,而这正是 PIT-5 特征;滤阈改由 skew 派生。3) device_now 返 None 静默不过滤,baseline 与 delta 未查 None;补判红。4) DeviceReader 漏登记 manifest 至批次 45 才发现,新流程无校验;把 sync_workspace_to_code.py 的 gen-manifest-only 配 check-only 加单测。

## 编辑（方向 1-4，全部 harness）
- 方向 1: mode_baseline trusted 滤空（仅剩时钟回拨前旧记录）→ 拒写基线返 1（照 mode_ts L273），max_ts 不再落 0（原 default=0 绕过零文件护栏致 delta 假绿）
- 方向 2: _trusted_tss 滤阈改由调用方 skew 派生（默认 600）；mode_ts/baseline/delta 传 args.skew（硬编码 300 < skew 600 时把 PIT-5 判红对象自身滤掉架空）
- 方向 3: mode_baseline/mode_delta 补 device_now() None 判红（静默不过滤等于放弃护栏）
- 方向 4: 新建 tests/test_sync_workspace_to_code.py 5 用例——gen-manifest-only + check-only 组合（不存在不写盘、已有不覆盖、无变化报 OK、source 映射、deletions 段）
- 单测: 154 全过（+7）

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → pass 1/1
### lcview-trigger → pass 8/8（trusted_tss skew 派生后 delta/ts 正常）

## 真值
- endpoint: 192.168.1.28:5555
- 时钟: trigger 前自动 clock_sync 校准（skew 1s）
- USB: DEV=1-2 VID=1256 PID=25344
