- schema_version: 1
- batch_id: 8cfe4f9cf98b
- batch_base: b82ea7739260
- verified_commit: b82ea7739260
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass liveness=8/8 pipeline=3/3 warn=pass(归档后) trigger=8/8(新daemon部署) unit=91/91 hal=18/18
- elapsed_s: 4200
- summary: 补 daemon 主循环与生产配置 UT 并清历史污染（warn/trigger 全绿、batch_parser 88.31% 入分母）

## body

## CDP 原文
-sv base:b82ea7739260
意图: 补 daemon 主循环与生产配置 UT 并清历史污染
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) lechao_lcview.cpp 与 main_lcview_hal.cpp 未编入任何测试,不在 72.63% 分母内等于隐性抬高;把 main 内循环抽可测函数入 filegroup 并补 UT 覆盖 getBatch 轮询与 HAL 死亡重连与 schema 加载失败。2) FileWriter.cpp 67.08% 是生产文件最大空洞,107 行未覆盖;补轮转边界与写失败与 reopen 路径 UT。3) 生产 config/lcview_events.json 不被任何 UT 加载,UT 全用内联 kValidJson;加一条用 SchemaParser 解析真配置校验 10 个事件定义。4) warn 因 501 条时钟回拨前记录持续判红;一次性归档到 uploaded 并保留卫生检查,该环境准备动作写进 SKILL。

## 编辑（方向 1-4）
- 方向 1: 新建 daemon/batch_parser.h/.cpp（parseBatch/loadSchemaWithRetry/waitForHal/rebindAfterError 从 main 抽取），filegroup 加入（main 循环进测试分母）；main 改用抽取函数；新建 DaemonLoop_test.cpp 13 UT（空批次/合法记录落盘/坏长度/过小/validate 失败/trailing 写 invalid、schema 重试失败/成功、waitForHal 重试/耗尽/rebind）
- 方向 2: FileWriter_test 补 4 UT（>= 阈值轮转边界、阈值-1B 不轮转、跨天 seq 重置、轮转目标已存在恢复 currentSize、写失败 clear+reopen+retry、reopen 失败 DROPPING）
- 方向 3: SchemaParser_test 加 ProductionConfig_LoadsAll10Events（/vendor/etc 真配置 eventCount==10 + id 4..13 全定义）
- 方向 4: 归档 5 个含未来 ts 文件到 uploaded/（toybox 32 位算术溢出用字符串截断 ${MAX%?????????}）；归档动作 + 陷阱（归档须重启 daemon——流指向被移走 inode 会继续写 uploaded/）写进 SKILL 环境准备段

## 部署与自愈
- 新 daemon（batch_parser 重构版）push /system/bin + 重启（旧进程流指向被归档 inode，logs/ 无新数据致 delta/fresh 假红——重启后恢复正常）
- 归档副作用记录: uploaded/ 5 文件含 daemon 后续追加数据（流指向移走 inode），重启后 daemon 写 logs/ 新文件

## 单测
- harness 167 全过；unit_test 91/91（+18）；hal_test 18/18

## 覆盖率（NATIVE_COVERAGE 上板实测，生产文件专项小计）
- daemon/batch_parser.cpp: 88.31%（新入分母，main 循环隐性抬高消除）
- daemon/SchemaParser.cpp: 83.90%（原 79.02）
- daemon/FileWriter.cpp: 68.00%（原 67.08）
- hal/DeviceReader.cpp: 61.90%
- hal/LcView.cpp: 81.53%
- 生产合计: 848 行 75.59%
- TOTAL: 2228 行 89.59%

## 验收逐项（归档 + daemon 重启后全绿）
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-liveness → pass 8/8（logfield 心跳字段断言）
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → **pass**（501 条历史污染归档后卫生检查判过）
### lcview-trigger → **pass 8/8**（新 daemon 部署后事件链路恢复）

## 真值
- endpoint: 192.168.1.28:5555
- 时钟: trigger 前自动校准
- USB: DEV=1-2 VID=1256 PID=25344
- 归档: 5 文件移入 uploaded/（logs 剩 17 正常文件）
