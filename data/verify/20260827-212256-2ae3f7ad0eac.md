- schema_version: 1
- batch_id: 2ae3f7ad0eac
- batch_base: f7715486acd9
- verified_commit: f7715486acd9
- verify_mode: board
- result: pass
- build: skip
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass liveness=8/8 pipeline=3/3 warn=fail(无新事件数据状态) trigger=8/8(值断言) transfer=5/5(dd失败暴露)
- elapsed_s: 1200
- summary: 撤错误护栏并做 lcview 收口审计（2 处假绿修复、事件登记归因修订）

## body

## CDP 原文
-sv base:f7715486acd9
意图: 撤错误护栏并做 lcview 收口审计
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) 上批方向 4 误判:收据淘汰是 cdp_receipt 的 _DETAIL_KEEP 50 配 prune_details 的设计老化,非事故;test_receipts_guard.py 断言只增不删与设计互斥必永久红,整文件删除,并在 workspace-verify SKILL 加一行:老化属设计,被淘汰收据不得当事故恢复。2) 事件 13 登记过宽:上批 usbreset 副作用记录 rate_degraded 出现,证明该打点 host 模式可达;verify-cases 盘点把 13 改为已观察待确定性触发,并为 6 与 7 与 10-12 写明归因是打点在 USBD 设备栈或 LcIod 侧而 host 不经过。3) 收口判据为可达事件全绿加判据无假绿:复核 verify-cases 五个 lcview 用例的每条判据,残留子串匹配或读不到即通过项改判红,结论写进收据。

## 编辑（方向 1-3）
- 方向 1: 确认 cdp_receipt.py _DETAIL_KEEP=50 + prune_details 为设计老化（收据超 50 份删最旧，trend.md 不计配额）——074627/111251 被淘汰系配额驱动非事故；删除 test_receipts_guard.py（只增不删断言与老化互斥必永久红）；SKILL 新增"收据老化（设计语义）"段：老化属设计，被淘汰收据不得当事故恢复
- 方向 2: verify-cases 事件盘点修订——13(rate_degraded) 改为"已观察待确定性触发"（usbreset 副作用实测出现，host 模式可达但无确定性注入）；6/7/10/11/12 写明归因（打点在 USBD 设备栈/LcIod 侧，xhci-host 不经过）
- 方向 3: 五用例逐条判据审计，修复 2 处假绿：
  - trigger authorized 切换：cat 输出值未断言（echo 成功但 authorized 值未变 → cmd exit 0 假绿）→ hostcmd 加 `| grep -q "^0$"/"^1$"` 值断言
  - transfer dd：`|| true` 吞失败（U 盘缺失时触发失败静默通过）→ 去掉（dd 失败即判红，delta 兜底为双保险）
  - 其余判据复核无假绿（读不到即红：log 空/锚点缺失/svc 非 running/boot 未完成/零记录/stat 失败/基线缺失全判红）

## 审计结论（方向 3，五用例逐条）
- lcview-liveness 8 判据：svc×2 需 running、log×2 空即红、logfield×3 锚点与字段缺失即红、boot 读不到即红——无假绿
- lcview-pipeline 3 判据：files/valid_json/schema 零记录/拉取失败/坏行全判红——无假绿
- lcview-pipeline-warn 2 判据：invalid stat 失败/非数字/非空判红；ts 无记录/未来记录/超窗判红——无假绿（本次判红=无新事件数据状态，非假绿）
- lcview-trigger 8 判据：authorized 值断言已补（假绿修复）；其余判红路径完备
- lcview-transfer 5 判据：dd 失败暴露（假绿修复）；delta 基线后无新增判红完备

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-liveness → pass 8/8
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → fail（ts 最新记录偏差 2550s——无新事件数据状态，卫生检查语义正确非假绿）
### lcview-trigger → **pass 8/8**（authorized grep 值断言生效）
### lcview-transfer → **pass 5/5**（dd 失败暴露后真实执行成功）

## 单测
- harness 167 全过（-2：撤除与老化设计互斥的护栏用例）
