- schema_version: 1
- batch_id: 2f8bc25f84a0
- batch_base: 20b4e5a3ea36
- verified_commit: 20b4e5a3ea36
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass liveness=8/8(logfield) pipeline=3/3 warn=fail(501条历史卫生检查,预期) trigger=8/8(ts baseline限定) hal_test=18/18 unit=73/73
- elapsed_s: 3000
- summary: 修心跳子串假绿与 ts 全历史判据并补 DeviceReader UT（trigger 8/8、DeviceReader 覆盖 0→61.90%）

## body

## CDP 原文
-sv base:20b4e5a3ea36
意图: 修心跳子串假绿与 ts 全历史判据并补 DeviceReader UT
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) liveness 的 log 加 overrun=0 是子串匹配,5000 行缓冲必命中开机初期零值心跳,故障累计后仍 PASS;新增 logfield 标签取锚点最后一行按字段名提数值比较,三条改用。2) trigger 的 ts 判全历史致 501 条时钟回拨前记录永久判红 7/8;trigger 的 ts 只判 baseline max_ts 之后的新记录,全历史卫生检查移入 warn。3) 覆盖率 TOTAL 84.33% 被测试源 95 到 100% 抬高,生产文件实为 67.08 与 79.02 与 81.53 与 0;收据加生产文件专项小计。4) DeviceReader.cpp 覆盖 0% 因被 Mock 顶替;加注入 fd 的可测缝并补 UT 覆盖 open 重试与 poll 超时与部分读与错误码。

## 编辑（方向 1-4）
- 方向 1: ws_acceptance 新增 logfield 标签（锚点|字段|比较符|数值，取 logcat 含锚点的最后一行按字段名提取数值比较）；liveness 三条 log:overrun=0 等改 logfield（5000 行缓冲命中开机初期零值心跳的假绿防住）
- 方向 2: mode_ts 支持显式 --baseline 限定（只判基线 max_ts 后新记录，未来记录只报告不判红——校准回拨遗留属历史污染）；全历史卫生检查移入 lcview-pipeline-warn（无 baseline 判全历史，501 条未来记录判红暴露）；trigger 的 ts 命令加 --baseline /tmp/lcview_baseline.json
- 方向 3: 收据加生产文件覆盖率专项小计（见下）
- 方向 4: EpollDeviceReader 加注入 fd 可测缝（explicit 构造 + open 幂等）；新建 DeviceReader_test.cpp 8 UT（open 成功/幂等、poll 超时、部分读、closed fd EBADF、参数防御 EBADF、ioctl 失败容错、close 幂等）；hal_test srcs 加入

## 单测
- harness 167 全过（+9）；hal_test 18/18（+8 DeviceReader UT）；unit_test 73/73

## 覆盖率（方向 3/4，NATIVE_COVERAGE 插桩上板实测，不设门禁）
- TOTAL: 1869 行 87.96% 覆盖（225 未覆盖），155 函数 95.48% 执行
- 生产文件专项小计（排除测试源与 AIDL 生成头）：
  - daemon/FileWriter.cpp: 行覆盖 67.08%（325 行，107 未覆盖）
  - daemon/SchemaParser.cpp: 行覆盖 79.02%（205 行，43 未覆盖）
  - hal/LcView.cpp: 行覆盖 81.53%（157 行，29 未覆盖）
  - hal/DeviceReader.cpp: 行覆盖 61.90%（84 行，32 未覆盖）——方向 4 注入 fd 可测缝生效，0% → 61.90%
  - 生产文件合计: 771 行，覆盖 560 行 = 72.63%
- 测试源: DeviceReader_test 100% / SchemaParser_test 100% / record_codec_test 100% / FileWriter_test 97.83% / LcView_test 95.68%

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-liveness → pass 8/8（logfield 锚点末行取真实累计值：overrun=0 dropped=0 readErr=0，上板命中）
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → fail（预期：全历史卫生检查检出 501 条时钟回拨前未来记录判红，非阻断 warn 项）
### lcview-trigger → pass 8/8（ts --baseline 限定只判本轮新记录，501 条历史污染不再判红；fresh/delta/probe 全过）

## 真值
- endpoint: 192.168.1.28:5555
- 时钟: trigger 前自动 clock_sync 校准
- USB: DEV=1-2 VID=1256 PID=25344
