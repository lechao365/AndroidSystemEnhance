- schema_version: 1
- batch_id: 7bdd5c43bfab
- batch_base: 89204b802dd1
- verified_commit: 89204b802dd1
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc_hal=pass svc_daemon=pass boot=pass liveness=8/8(含overrun/dropped/readErr=0) pipeline=3/3 warn=1/1 trigger=7/8(ts判红501条历史未来记录,方向1修复实证)
- elapsed_s: 3600
- summary: 引覆盖率度量并加固心跳与 ts 判据（覆盖84.33%、liveness 8/8、ts 未来记录判红实证）

## body

## CDP 原文
-sv base:89204b802dd1
意图: 引覆盖率度量并加固心跳与 ts 判据
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) mode_ts 未来向仍不可达:滤阈等于判红阈致超 now 加 skew 的被丢弃余下恒过,实测 8h 未来记录静默 PASS;改被滤条数非 0 即判红,与最新偏差分开报。2) liveness 只 grep 心跳子串,overrun 与 dropped 与 readErr 从不校验;加从 logcat 提字段值断言 0 的判据。3) mode_invalid 在 rc 非 0 或 size 非数字时 return 0 静默过;改判红并加坏记录正向验证。4) lcview tests/Android.bp 两个 cc_test 加 native_coverage,用 NATIVE_COVERAGE 编译上板跑后 pull gcda 出每文件行覆盖写进收据,不设门禁。5) 内核与用户态 lcview_events.h 的 MAGIC 与 TYPE 与事件 id 与 hdr 一致性加单测。

## 编辑（方向 1-5）
- 方向 1: _trusted_tss 改返 (trusted, filtered_count)；mode_ts 被滤条数非 0 即判红（与最新偏差分开报）——滤阈=判红阈时超阈未来记录被丢弃余下恒过的架空消除
- 方向 2: verify-cases lcview-liveness 加 log:"overrun=0" log:"dropped=0" log:"readErr=0" 判据（心跳字段值断言 0，故障累计留痕即暴露）
- 方向 3: mode_invalid stat rc 非 0 / size 非数字 → 判红（原"视为空通过"假绿）；非空时展示坏记录内容正向验证
- 方向 4: tests/Android.bp 两 cc_test 加 native_coverage: true；按 AOSP 官方 CLANG_COVERAGE=true + NATIVE_COVERAGE_PATHS 链路编译（profraw 非 gcda），上板跑后 pull profraw → llvm-profdata merge → llvm-cov report 每文件行覆盖
- 方向 5: 新建 tests/test_lcview_events_consistency.py——内核/用户态 lcview_events.h 的 MAGIC/TYPE/事件 id/hdr 字段序列/16B 头一致性（6 用例）
- 单测: 158 全过（+8）

## 覆盖率（方向 4，NATIVE_COVERAGE 插桩上板实测，不设门禁）
- TOTAL: 147 函数 89.80% 执行；1819 行 84.33% 覆盖（285 行未覆盖）
- FileWriter.cpp: 行覆盖 67.08%（107 未覆盖）
- SchemaParser.cpp: 行覆盖 79.02%（43 未覆盖）
- LcView.cpp: 行覆盖 81.53%（29 未覆盖）
- DeviceReader.cpp: 0%（测试用 MockDeviceReader 替代，未直接执行）
- 测试文件: FileWriter_test 97.83% / LcView_test 95.68% / SchemaParser_test 100% / record_codec_test 100%

## 验收逐项
### 批次验收（svc hal/daemon + boot）→ pass 3/3
### lcview-liveness → pass 8/8（新增 overrun=0/dropped=0/readErr=0 判据上板命中，心跳字段全 0 链路健康）
### lcview-pipeline → pass 3/3
### lcview-pipeline-warn → pass 1/1
### lcview-trigger → fail 7/8（ts 判红）
- delta/fresh 全过（链路健康，事件正常产生）
- ts --skew 600: FAIL——"检测到 501 条时钟回拨前未来记录（ts > now+600s，PIT-5 特征，判红）"
  分析: 方向 1 修复生效的实证——上批时钟校准（超前 7h 回拨）前写入的 501 条历史记录 ts 永超当前时钟，被判红暴露；属预期新行为 + 设备历史数据状态，非链路故障（delta 增量与 fresh 均过证明事件链路正常）；历史记录待时间自然追平或清理
