- schema_version: 1
- batch_id: manual-2608262221
- batch_base: dev
- verified_commit: dev
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc:lechao_lcview_hal=pass svc:lechao_lcview=pass log:"LcView: alive beat="=pass log:"heartbeat, loop="=pass boot=pass
- elapsed_s: 3600
- summary: lcview CXX 修复批首次真实上板验证通过（7P1 全清，设备健康运行）

## body

# workspace-verify 首次真实上板验证（lcview CXX 修复批次）

## 背景
dev 分支 lcview 源码首次真实端到端上板验证。全量检视（3 子 agent 并行，CXX-001~004）发现 P0=0 / P1×7 / P2 若干。

## 修复内容（code 源头，19 文件同步 workspace）
- P1 CXX-003: SchemaParser JSON 字段 isMember+isXxx 前置校验（防 schema 损坏 SIGABRT）
- P1 CXX-002: FileWriter openFile fstat 恢复 currentSize（重启后轮转约束生效）
- P1 CXX-002: FileWriter failbit 粘滞自愈（clear+reopen+重试）
- P1 CXX-004: daemon HAL 失联 exit(1) + rc 去 oneshot（init 重启链恢复）
- P1 CXX-004: HAL readerLoop fatalExit 4 步退出（置标志→交付残留→ERROR→exit(1)）
- P1 CXX-004: HAL getBatch reader 死亡返回 STATUS_DEAD_OBJECT（禁伪装空数据）
- P1: hal rc 去 oneshot
- 重构: DeviceReader 抽象（EpollDeviceReader 生产 / MockDeviceReader 测试注入）+ LcView 构造注入 + start() 时序（先注册后启线程）
- P2: writeInvalid 转义+hex、残留字节落盘、seq 一致、retention 保护 invalid、重复 id 警告、心跳带 overrun/dropped、ET/LT 注释修正、内核字节序注释、sepolicy 补 logd
- 测试适配: LcView_test 分支 3/GetBatch 改死亡语义 + makeLcView start()

## 验证过程
1. 编译: make lechao_lcview_unit_test lechao_lcview_hal_test + 4 部署模块 全通过
2. push 6 产物（bin×2 rc×2 vintf config）→ reboot（22:15:15）
3. 验收 lcview-liveness（svc×2 + file + boot）首轮: file 假红（0750 system-only 目录 shell 无权限）
4. 设备时钟漂移（06-23 vs 08-26）→ date 修正
5. 用例资产改为心跳 log 判据（持续性日志，非重启场景可靠）→ 5/5 pass

## 现场证据（设备 logcat 摘录）
- LcView: alive beat=570 buffered=0B overrun=0 dropped=0 readOk=411 readEmpty=159 readErr=0 flush=3
- lechao_lcview: heartbeat, loop=180, kernel overrun=0
- avc denied 总数 = 0（daemon logd 权限误报确认：system 域有日志写权限）
- 日志文件持续写入（mtime 与运行时刻同步）
