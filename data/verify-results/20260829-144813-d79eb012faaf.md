- schema_version: 1
- batch_id: d79eb012faaf
- batch_base: eaaa7c811aa9
- verified_commit: eaaa7c811aa9
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: {"overall":"pass","items":[
{"tag":"svc:lechao_lcview","status":"pass","detail":"init.svc.lechao_lcview=running (PID 4616, build=LCVIEW-VERIFY-20260829-04)"},
{"tag":"log:\"heartbeat, loop=\"","status":"pass","detail":"logcat 命中 heartbeat(新进程 4616 loop=60)"},
{"tag":"logfield:\"heartbeat, loop=|overrun|=|0\"","status":"pass","detail":"overrun=0"},
{"tag":"logfield:\"heartbeat, loop=|dropped|=|0\"","status":"pass","detail":"dropped=0"},
{"tag":"logfield:\"heartbeat, loop=|readErr|=|0\"","status":"pass","detail":"readErr=0"},
{"tag":"hostcmd:\"cases/lcview_check.sh --mode conserve\"","status":"pass","detail":"守恒成立: 窗口产生 0 ≈ 落盘 0 (上限16)"},
{"tag":"boot","status":"pass","detail":"sys.boot_completed=1"}
]}
- elapsed_s: 0
- summary: lcview 可维护性重构(删除对齐/假红修正/死代码/TLV 统一解码/拆函数/SKILL 约束) 全量验证通过
- metrics: 

## body

# 批次正文（CDP 原文）

-sv base:eaaa7c811aa9
意图: lcview 可维护性重构
验收: svc:lechao_lcview boot
方向: 1) sync_code_to_workspace 只 RESTORE 不删文件,code 删后 workspace 残留致 soong 报重复定义,改删除对齐。2) device.mk 已应用 diff 却标 RESIDUAL 假红,须修正。3) FileWriter.cpp L199 closeFile 与 SchemaParser.cpp L52 reload 零调用点,连声明一并删。4) 拆超长函数 lechao_lcview.cpp main 157 行与 parseJson 126 行,行为不变。5) FileWriter.cpp formatJsonLine 与 SchemaParser.cpp validate 的 TLV switch 重复,统一为单一解码器。6) cross-device-emit SKILL.md 约束段补两条: 一轮只交付一个 cdp_parse 返 exit 0 的批次,草稿禁出现;确认须在交付批次前提。

# 阶段明细

## 编辑（apply 侧）
- 方向1: sync_code_to_workspace.py 删除对齐。EXTRA-NEW-TRACKED/UNTRACKED 动作由 sync(checkout base) 改为 delete(删除残留),修复 base 中无此文件时 checkout 必失败且不删除的问题;EXTRA-MODIFIED 保持 checkout(恢复 base 正确)。_do_sync_extra 新增目录/文件删除分支(修 str.is_dir bug),plan 头注释更新,动作表加 delete。
- 方向2: device.mk.diff 第3/4 hunk 的 new 起始行号 233/248 修正为 229/244(与 workspace 真实 git diff 完全一致)。根因:上批删 PRODUCT_PACKAGES hal 行时只改第2 hunk 计数未更新后续 hunk 行号,致 _diff_normalized 比对失败恒标 RESIDUAL 假红。
- 方向3: 删 FileWriter::closeFile(FileWriter.cpp L199+FileWriter.h 声明)、SchemaParser::reload(SchemaParser.cpp L52+SchemaParser.h 声明+文件头注释),均零调用点。
- 方向4: 拆超长函数。lechao_lcview.cpp main 157行→runMainLoop 抽主循环体+main 仅初始化(约60行);SchemaParser.cpp parseJson 126行→parseEventDef 抽单事件解析。行为不变。BUILD_TAG 升 04。
- 方向5: 新建 daemon/record_codec.{h,cpp} 统一 TLV 解码器 decodeRecordField,validate 与 formatJsonLine 共用。修两处单测失败:STRING 数据越界保留 "exceeds record" 消息(解码器 valueLen 区分前缀缺失/数据越界);unknown type 输出 null 继续(非 drop)。daemon/Android.bp filegroup 加 record_codec.cpp。
- 方向6: cross-device-emit SKILL.md 约束段补两条:一轮只交付一个 cdp_parse exit 0 批次/草稿禁出现;确认须在交付批次前提。
- manifest.yaml 重生成(record_codec.h/cpp 纳入)。

## 同步与编译
- sync_code_to_workspace --auto: 8 条 RESTORE 全 FIXED, RESIDUAL=0, NEW-DIFF=0(方向1/2 修复后 device.mk 不再假红)。
- m lechao_lcview_unit_test: 编译通过; 设备跑 114 tests PASS。
- m lechao_lcview vendor.lechao.lcview-config: 编译通过。
- ws_upload_tests: lechao_lcview_unit_test PASS(114), lechao_lciod_unit_test PASS(35), lechao_lciod_hal_test PASS(28)。

## 部署与验收
- 部署: /vendor/bin/lechao_lcview + rc + lcview_events.json(vendor remount 后 push + restorecon)。
- 服务重启: PID 4616, build=LCVIEW-VERIFY-20260829-04。
- 新进程心跳: loop=60, overrun=0, dropped=0, readErr=0, total_records=3166, jsonl_records=0(重启后无新事件), avg_format_us=0(未写)。
- lcview-liveness 全 pass: svc=running, heartbeat 命中, overrun/dropped/readErr=0, conserve 守恒成立(窗口产生0≈落盘0), boot_completed=1。

## 回滚步骤
- 回滚: 拉取上批二进制恢复: 需从上一 commit(eaaa7c811aa9) 产物恢复 /vendor/bin/lechao_lcview 与 /vendor/etc/init/lechao_lcview.rc、/vendor/etc/lcview_events.json, restorecon 后 stop/start lechao_lcview。
- 或 git revert 本批 commit 后重走 workspace-verify 重新部署。

## 偏差说明
- 无。方向5 重构后两处单测(UnknownType/StringFieldDataExceeds)初跑失败,根因:统一解码器改变了 errMsg 文本(原 "exceeds record")与 unknown 处理(原输出 null),已修复(保留原文本+unknown 输出 null),114 tests 全 pass,行为与上批一致。
