- schema_version: 1
- batch_id: 9056a45555e6
- batch_base: 0b4d1225f465
- verified_commit: 0b4d1225f465
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: svc:lechao_lcview boot
- elapsed_s: 5400
- summary: 清理 HAL 残留并建写路径指标（CDP 9056a45555e6）: HAL/ILcView/V2-ndk 全退役，push 映射补登记，avg_format_us=2 avg_write_us=1，liveness pass
- metrics: {"drain_s": 1.085, "latency_ms_per_event": 0.353, "daemon_rss_kb": 5208, "throughput_evs": 1270.3, "avg_format_us": 2, "avg_write_us": 1}

## body

# CDP 批次原文（batch-20260829-122207.cdp，batch_id 3e8f0eaf3f2f，base 0b4d1225f465）

-sv base:0b4d1225f465
意图: 清理 HAL 残留并建写路径指标
验收: svc:lechao_lcview boot
方向: 1) libjsoncpp.so 与 sepolicy 产物 vendor_file_contexts 与 precompiled_sepolicy 均靠手工推送,未进 verify-cases 的 push 映射,清刷必炸,与 V2-ndk 漏推同源;补登记。2) 直读路径已板上跑绿,做清理:删 HAL 二进制与 .disabled rc 与 VINTF fragment 与 service_contexts 条目与 PRODUCT_PACKAGES 入口,ILcView 与 V2-ndk 退役,hal_test 仍有效逻辑并入 daemon 测试。3) drain 被攒包策略钉死在 1.07s,对写路径成本不敏感;在 daemon 内累计 writeRecord 与 formatJsonLine 的耗时与次数,心跳出平均微秒每条,作为微优化的可判定指标。4) enforceRetention 每轮主循环全目录 opendir 加 stat,空批也扫;改按轮转或写入计数降频。

# 编辑与部署要点

方向 1（push 映射补登记）:
- verify-cases.yaml lcview 模块 push 段新增 libjsoncpp.so → /vendor/lib64/libjsoncpp.so、
  vendor_file_contexts → /vendor/etc/selinux/vendor_file_contexts、
  precompiled_sepolicy → /vendor/etc/selinux/precompiled_sepolicy（与 V2-ndk 漏推同源，
  清刷必炸；上批 libjsoncpp 实拍 linker CANNOT LINK 靠手工补推）

方向 2（HAL 清理）:
- 删 hal/ 全部：Android.bp（lechao_lcview_hal cc_binary + VINTF prebuilt_etc）、
  LcView.h/.cpp、main_lcview_hal.cpp、lechao_lcview_hal.rc.disabled、
  vendor.lechao.lcview.ILcView-service.xml
- 删根 Android.bp（aidl_interface vendor.lechao.lcview）与 aidl_api/、vendor/lechao/lcview/ILcView.aidl
  → ILcView / V2-ndk 退役
- 删 device.mk.diff PRODUCT_PACKAGES 的 lechao_lcview_hal、vendor.lechao.lcview.ILcView-vintf 入口
- 删 service_contexts.diff 的 vendor.lechao.lcview.ILcView/default 条目
- 删 sepolicy/lechao_lcview_hal.te；hal_device 类型并入 lechao_lcview.te（daemon 直读仍需）
- file_contexts.diff 删 /vendor/bin/lechao_lcview_hal → lechao_lcview_hal_exec 条目
  （/dev/vendor_lechao_lcview 节点标签保留）
- headers 库 vendor.lechao.lcview_headers 原定义于根 Android.bp（已删），迁入
  include/Android.bp 独立定义（Soong srcs/export_include_dirs 禁 .. 路径）
- README.md 映射表更新（删 hal te 行、lcview 说明改直读）

方向 2（hal_test 有效逻辑并入 daemon 测试）:
- 删 tests/LcView_test.cpp 与 hal_test cc_test 模块
- flush 触发语义（满/超时/滞留窗）抽 shouldFlushBatch() 入 batch_parser，
  DaemonLoop_test 新增 3 用例覆盖（Flush_EmptyBuffer_NeverFlushes /
  Flush_BufferFull_TriggersFlush / Flush_TimeoutOrAge_TriggersFlush）

方向 3（写路径指标）:
- FileWriter 新增 WriteTimings：formatJsonLine 与 writeRecord 各自累计次数+耗时（us）
- daemon 心跳追加 avg_format_us / avg_write_us（平均微秒每条）
- 实测（LCVIEW-VERIFY-20260829-03）：avg_format_us=2 avg_write_us=1

方向 4（enforceRetention 降频）:
- FileWriterConfig 新增 retentionScanEveryWrites=256（按写入计数触发扫描，
  空批轮次不再 opendir+stat）；=0 关闭降频（单测显式调用场景）
- FileWriter 内 mWritesSinceRetention 累计，enforceRetention 未达阈值提前返回
- 6 个 FileWriterRetentionTest 的 cfg 设 retentionScanEveryWrites=0 适配

# 实测发现（本批偏差/坑）

- workspace 侧删除对齐缺失（同源历史问题）：code 删 hal/aidl 后 workspace 仍残留，
  sync_code_to_workspace 只 RESTORE 不删文件——手动 rm 对齐（hal/ aidl_api/ vendor/
  根 Android.bp LcView_test.cpp）；若不复位则 soong 报 module 重复定义。
- device.mk RESIDUAL 误报：workspace device.mk 内容已正确（diff 应用生效），
  校验器仍标 RESIDUAL——文件内容实际一致，编译验证通过。
- Soong export_include_dirs 禁 ../ 越界路径：headers 库迁 include/ 目录独立定义。

# 验收结果

lcview-liveness 全 pass:
- svc:lechao_lcview running（HAL 进程不存在）
- log "heartbeat, loop=" 命中；logfield overrun=0 / dropped=0 / readErr=0
- conserve 守恒成立；sys.boot_completed=1

lcview-perf（对比上批基准 0b4d122: drain 1.071s / 延迟 0.348ms / RSS 5140kB / 吞吐 1270.8ev/s）:
- drain 1.085s（+1.3%）
- 延迟 0.353 ms/event（+1.4%）
- RSS 5208 kB（+1.3%）
- 吞吐 1270.3 ev/s（≈持平）
- 写路径指标实测 avg_format_us=2 / avg_write_us=1：格式化+写盘每条约 3µs，
  印证 drain 由攒包/落盘策略主导而非格式化路径（方向 3 判据落定）

设备侧单测 3 目标全 pass：lechao_lcview_unit_test 114 例（含 shouldFlushBatch×3）、
lechao_lciod_unit_test 35 例、lechao_lciod_hal_test 28 例。

# 部署顺序与回滚

部署（本批）:
1. adb root; adb remount
2. push /vendor/bin/lechao_lcview + /vendor/etc/init/lechao_lcview.rc +
   /vendor/etc/lcview_events.json + /vendor/lib64/libjsoncpp.so
3. push /vendor/etc/selinux/vendor_file_contexts + precompiled_sepolicy
4. rm /vendor/bin/lechao_lcview_hal、/vendor/lib64/vendor.lechao.lcview-V2-ndk.so、
   /vendor/etc/vintf/manifest/vendor.lechao.lcview.ILcView-service.xml、
   /data/local/tmp/lcview_rc_backup
5. restorecon /vendor/bin/lechao_lcview /vendor/lib64/libjsoncpp.so
6. reboot；daemon 直读启动，HAL 不再存在

回滚（仅靠 adb，零砖机）:
1. 从 git 基线（本批 commit 的父 0b4d122 即上批）恢复 HAL：二进制 lechao_lcview_hal、
   rc lechao_lcview_hal.rc.disabled 需从镜像/仓库产物回放（本批已删设备文件，
   可 git checkout 上批产物重建或重编）
2. 恢复 VINTF fragment 与 service_contexts 条目、PRODUCT_PACKAGES 入口（git 基线回退）
3. 恢复 device.mk / sepolicy（git checkout 上批）→ 重编 vendor 分区镜像刷写
   （注意：本批改动涉及 device.mk/sepolicy，回滚需重编镜像，非纯 adb push 可逆；
   但 daemon 二进制仍为直读版可用，HAL 恢复需镜像级回退）
