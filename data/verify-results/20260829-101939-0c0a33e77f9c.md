- schema_version: 1
- batch_id: 0c0a33e77f9c
- batch_base: 5af3cdd911fd
- verified_commit: 5af3cdd911fd
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: {"overall":"pass","items":[
{"tag":"lcview-liveness","status":"pass","detail":"svc x2 + alive beat + heartbeat + overrun/dropped/readErr=0 + conserve + boot 9/9"},
{"tag":"lcview-perf","status":"pass","detail":"吞吐 1165.9 ev/s 延迟 0.348ms RSS 5616kB drain 1.070s"},
{"tag":"lcview-pipeline","status":"pass","detail":"files 55 文件 / valid_json 33610 条 0 坏行 / schema 0 不匹配"},
{"tag":"lcview-pipeline-warn","status":"pass","detail":"invalid 空 / ts skew 17.5s<600s"},
{"tag":"lcview-trigger","status":"pass","detail":"authorize 切换 + disconnect id9 + probe 新增 + fresh + ts 8/8"},
{"tag":"lcview-transfer","status":"pass","detail":"事件 4/5 delta 触发"},
{"tag":"lcview-recover-hal","status":"pass","detail":"kill 后拉起+心跳恢复"},
{"tag":"lcview-recover-daemon","status":"pass","detail":"拉起+心跳+落盘 68 条+轮转 seq 3->4"},
{"tag":"lciod-liveness","status":"pass","detail":"svc x2 + monitor 日志 + boot"},
{"tag":"lciod-pipeline","status":"pass","detail":"stats 校验 1 设备"},
{"tag":"lciod-trigger","status":"pass","detail":"read/write delta 严格增"}]}
- elapsed_s: 0
- summary: daemon 迁入 vendor（vendor:true + rc/file_contexts 迁移），全量用例 pass；架构前基准：吞吐 1165.9ev/s 延迟 0.348ms RSS 5616kB drain 1.070s；recover-daemon 脚本 max_seq 改当天过滤修复历史文件污染
- metrics: {"daemon_rss_kb": 5616, "dd_s": 2.635, "drain_ms_per_event": 0.348, "drain_s": 1.07, "load_mb": 64, "throughput_evs": 1165.9}

## body

# verify 收据 body — batch 0c0a33e77f9c

## CDP 原文
```
-sv base:5af3cdd911fd
意图: daemon 迁入 vendor 并采架构前基准
验收: svc:lechao_lcview_hal svc:lechao_lcview boot
方向: 1) daemon 在 system 而 HAL 在 vendor,此分割是 HAL 唯一存在理由;daemon/Android.bp 加 vendor true,rc 的 service 路径由 /system/bin 改 /vendor/bin,file_contexts 的 exec 标签同步迁移,不动数据路径代码。2) verify-cases 的 modules.lcview.push 仍指 system bin 与 system etc/init,不改则新产物无法部署,已放行。3) 远端操作须零砖机:本批无内核改动,严禁刷 boot.img;旧 system 侧产物留作禁用态,回滚须仅靠 adb 恢复 rc 加 reboot,收据写明步骤。4) 采架构前基准:lcview-perf 的 drain 与 RSS 及全部现有用例结果记入收据,作为下阶段撤 HAL 的对照。
```

## 方向落实
### 1) daemon 迁入 vendor（HAL 唯一存在理由 = 分割）
- `daemon/Android.bp`：lechao_lcview cc_binary 加 `vendor: true`（注释同步改为 vendor 分区）
- `daemon/lechao_lcview.rc`：service 路径 `/system/bin/lechao_lcview` → `/vendor/bin/lechao_lcview`
- `sepolicy/file_contexts.diff`：`/system/bin/lechao_lcview` → `/vendor/bin/lechao_lcview`（exec 标签同步迁移）
- `sepolicy/lechao_lcview.te`：lechao_lcview_exec 加 `vendor_file_type`（与 lcview_hal_exec 对齐），注释更新
- 数据路径代码零改动（schema 路径 /vendor/etc/lcview_events.json、数据目录 /data/vendor/lechao_lcview/ 均未动）

### 2) verify-cases.yaml modules.lcview.push 迁移
- lechao_lcview：/system/bin + /system/etc/init → /vendor/bin + /vendor/etc/init
- vendor.lechao.lcview-V2-ndk：/system/lib64 一份移除（daemon 已无 system 侧消费者），保留 /vendor/lib64
- 注释同步更新（daemon 与 HAL 均 vendor 落 /vendor）

### 3) 零砖机远端操作 + 回滚步骤（收据记录）
- 本批无内核改动，未触碰 boot.img/内核产物
- 部署动作（已完成）：
  1. adb root + adb remount
  2. push 新 daemon → /vendor/bin/lechao_lcview
  3. push 新 rc → /vendor/etc/init/lechao_lcview.rc
  4. push vendor.lechao.lcview-V2-ndk.so → /vendor/lib64/
  5. 旧 system 侧产物留作禁用态：/system/etc/init/lechao_lcview.rc 改名 .disabled（init 只加载 *.rc，旧 rc 不再声明 service；旧 /system/bin/lechao_lcview 二进制保留未删）
  6. reboot（未刷 boot.img）
- 回滚步骤（如需回退，仅靠 adb，零刷写）：
  1. adb root + adb remount
  2. `adb shell mv /system/etc/init/lechao_lcview.rc.disabled /system/etc/init/lechao_lcview.rc`（恢复旧 rc，指向 /system/bin/lechao_lcview）
  3. 可选：禁用 vendor 新 rc（mv /vendor/etc/init/lechao_lcview.rc 改名）避免同名 service 冲突
  4. adb reboot
  5. 校验：svc:lechao_lcview running + /system/bin 旧 daemon 心跳

### 4) 架构前基准（下阶段撤 HAL 对照）
- lcview-perf：事件吞吐=1165.9 events/s（dd 64MB / 2.635s），平均落盘延迟=0.348 ms/event，daemon RSS=5616 kB（VmHWM），drain=1.070s
- 全部现有用例结果见下方验收逐项

## 编辑后校验
- cdp_validate_patch.py file_contexts.diff → exit 0
- sync_workspace_to_code.py --auto：4 条 applied，落盘校验 fixed=4 residual=0 new_diff=0
- manifest.yaml 无变化（无文件增删）

## 编译
- make lechao_lcview_unit_test lechao_lcview_hal_test lechao_lciod_unit_test lechao_lciod_hal_test -j$(nproc) → success
  （system 侧旧产物自动移除：system/bin/lechao_lcview、system/etc/init/lechao_lcview.rc、system/lib64/V2-ndk）
- m lechao_lcview lechao_lcview_hal vendor.lechao.lcview.ILcView-vintf vendor.lechao.lcview-config → success
  （lechao_lcview 产物落 out/target/product/rpi5/vendor/bin/，system/bin 侧无产物）

## 设备侧单测（nativetest 真跑，ws_upload_tests.py）
- lechao_lcview_unit_test: PASS（105 tests）
- lechao_lcview_hal_test: PASS（20 tests）
- lechao_lciod_unit_test: PASS（35 tests）
- lechao_lciod_hal_test: PASS（28 tests，root 运行）
- 4 目标全部通过

## 部署与重启
- reboot 后 4 服务全 running（lechao_lciod_hal/lcview_hal/lciod/lcview）
- daemon SELinux 域 u:r:lechao_lcview:s0（新 vendor 二进制生效，进程 550 心跳含守恒三数 total=90 overrun=0 jsonl=90）

## 验收逐项（全部用例 pass）
- lcview-liveness：9/9 pass（svc x2 + alive beat + heartbeat + overrun/dropped/readErr=0 + conserve + boot）
- lcview-perf：pass（吞吐 1165.9 ev/s，延迟 0.348 ms，RSS 5616 kB，drain 1.070s）
- lcview-pipeline：3/3 pass（files 55 文件 / valid_json 33610 条 0 坏行 / schema 0 不匹配）
- lcview-pipeline-warn：2/2 pass（invalid 空 / ts skew 17.5s < 600s）
- lcview-trigger：8/8 pass（authorize_off→disconnect id9 新增 1 / authorize_on→probe 新增 / fresh / ts）
- lcview-transfer：pass（事件 4/5 delta 触发）
- lcview-recover-hal：pass（kill → init 拉起 + 心跳恢复）
- lcview-recover-daemon：pass（kill → 拉起 + 心跳 + 新事件落盘 68 条 + 轮转 seq 递增 3→4）
- lciod-liveness：3/3 pass（svc x2 + monitor 日志 + boot）
- lciod-pipeline：pass（stats 校验 1 设备）
- lciod-trigger：pass（read/write delta 严格增）

## 失败现场与自愈（1 次）
- lcview-recover-daemon 首跑 fail：轮转 seq 断言 5->5
- 根因：**用例脚本缺陷非产品回归**——lcview_recover.sh 的 max_seq() 全局取所有 jsonl 最大 seq，
  被 20260625 时钟漂移期历史文件（_p5）污染；产品 nextSeqFor 按 {event}_{name}_{当天日期}_p{seq}
  当天续接（跨天重置 seq=0），今天 _p0→_p3 递增续接行为正确（本批未动 FileWriter/恢复脚本）
- 修复：max_seq() 改为按设备当天日期（date +%Y%m%d）过滤，与产品续接语义一致
- 修复后重跑：seq 3->4 递增，pass
