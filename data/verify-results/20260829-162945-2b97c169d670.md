- schema_version: 1
- batch_id: 2b97c169d670
- batch_base: 5cdf0dcb1f0a
- verified_commit: 5cdf0dcb1f0a
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: {"overall":"pass","items":[{"tag":"svc:lechao_lcview","status":"pass","detail":"init.svc.lechao_lcview='running' exit=0"},{"tag":"boot","status":"pass","detail":"sys.boot_completed='1'"},{"tag":"conserve v4","status":"pass","detail":"静止确认起点无积压+负载窗口产生201≈落盘201守恒成立"},{"tag":"设备单测","status":"pass","detail":"lcview 127 / lciod 35 / lciod_hal 28 全 PASS(含新鲜度校验)"}]}
- elapsed_s: 0
- summary: 恢复 conserve 负向判红(v4 两段式采样,起点无积压负向为真异常)+ws_upload_tests 推送前校验产物新鲜度防旧二进制报绿+FileWriter writeLineFlush 写前记录偏移失败回退重试坏行归零,单测 127/35/28 全过
- metrics: 

## body

# 批次正文（CDP 原文）

-sv base:5cdf0dcb1f0a
意图: 恢复守恒负向检测并修重试半行
验收: svc:lechao_lcview boot
方向: 1) conserve 负向判红被整体放行,重复落盘与计数异常从此不可检测,收据称由心跳 dropped 替代不成立(dropped 计丢弃不计重复);零记录守卫亦放宽为 produced 与 landed 同为 0。conserve 已能自造负载,改为先等两拍增量归零确认静止再造负载采样,窗口起点无积压则负向为真异常,恢复判红。2) ws_upload_tests 首推旧测试二进制却报 PASS 114,未含新增 13 例,手工推新产物后才 127 全过;制度化设备单测通道能在不执行新用例时报绿,须校验推送产物与源码一致。3) FileWriter.cpp writeLineFlush 重试前不回退偏移,首次 flush 部分落盘后以 app 重开并重写整行,磁盘留半行加整行的坏行;写前记录偏移,失败后回退再重试,扩展已有 4 例 flush 失败直测验证坏行归零。

# 阶段明细

## 编辑（apply 侧）
- 方向1: lcview_check.py mode_conserve 改 v4 两段式采样——静止确认段（两拍增量归零 → 起点无积压 at_rest）+ 负载采样段（--conserve-load-mb 自造负载后两拍采样）;负向（in_flight < -limit）在 at_rest 时判红（重复落盘/计数异常,dropped 计丢弃不计重复不能替代）,仅起点有积压（追赶期）放行;零记录守卫恢复为 produced==0 判红（自造负载下应有数据可校验）。verify-cases.yaml 注释同步（注意5/注意6/lcview-transfer）。
- 方向2: ws_upload_tests.py 推送前校验产物新鲜度——verify-cases.yaml modules 增 test_src（源码目录相对 AOSP 根）,binary_is_stale 比对二进制与源码目录内最新文件 mtime,二进制早于源码即陈旧判红禁止报绿;新增 --aosp-root 参数,run_one 校验。单测补 TestBinaryIsStale 3 例 + TestRunOne 陈旧拒推 1 例 + load_test_targets src_map 2 例。
- 方向3: FileWriter.cpp writeLineFlush 写前记录偏移（writeBase=fs.currentSize,即上次成功后落盘字节数）,flush 失败恢复路径先 rollbackFileTo（open+ftruncate 截断到 writeBase,清首写部分落盘残留半行）再重开重试;FileWriter.h 注释同步。FileWriter_test.cpp 扩展 4 例直测验证坏行归零（NormalWrite 精确内容 / ReopenFail 无残留 / RetryFail 无虚增 / RecoverySucceeds 模拟半行残留被回退截断后整行只写一次）。
- 自愈 1 例: 新增 RecoverySucceeds 直测首版 fwrite 字面量越界（9 字节写 8 字节缓冲）被 FORTIFY 拦截 abort,改 std::string 精确长度后 127 全过。
- manifest.yaml 重生成: 无变化（仅内容改动,文件清单不变）。

## 同步与编译
- sync_code_to_workspace --auto: 首轮 3 条 RESTORE 全 FIXED（FileWriter.cpp/h + FileWriter_test.cpp）,第二轮 1 条（测试修复后重同步）,RESIDUAL=0, NEW-DIFF=0。
- make lechao_lcview_unit_test: 编译通过（127 tests）。
- make lechao_lcview vendor.lechao.lcview-config: 编译通过（仅 1 warning 既有）。

## 部署与设备单测
- push /vendor/bin/lechao_lcview（remount 后）+ stop/start,新进程 PID 9546 running。
- ws_upload_tests（含新陈旧校验）: lechao_lcview_unit_test PASS(127), lechao_lciod_unit_test PASS(35), lechao_lciod_hal_test PASS(28),三目标全绿。
- 方向2 制度化通道实测: 最新二进制（重构后 572872 字节）通过新鲜度校验（源码 mtime <= 二进制 mtime）正常推送报绿;旧二进制场景由单测 TestRunOne.test_stale_binary_rejected_before_push 覆盖（判红拒推）。

## 验收（方向1 conserve v4 设备实测）
- 主验收 svc:lechao_lcview boot: pass（init.svc.lechao_lcview='running' exit=0, sys.boot_completed='1'）。
- conserve v4 直跑（--conserve-load-mb 4）: 静止确认 前段增量 产生=0 落盘=0（起点无积压）;负载窗口 5s 内核产生增量=201,落盘增量=201,在途差值=0（上限 16）;OK 守恒成立——两段式采样与 at_rest 判定在设备侧真实生效。
- 负向判红恢复: 代码+单测覆盖（at_rest 时落盘>产生判红 / 追赶期放行）,设备侧负向路径需重复落盘故障注入,经代码审查确认（dropped 计丢弃不计重复,本判据不可替代）。

## 回滚步骤
- 回滚: git revert 本批 commit 后重走 workspace-verify 重新部署（恢复 conserve v3 放行语义 + 去掉新鲜度校验 + 去掉回退偏移逻辑）。
- 或拉取上一 commit(5cdf0dcb1f0a) 产物恢复 /vendor/bin/lechao_lcview, stop/start 重启。

## 偏差说明
- 方向1 负向判红设备侧未做故障注入实拍（需人为制造重复落盘,手段无确定性注入）,以单测 test_conserve_duplicate_landing_fails（at_rest 判红）与 test_conserve_negative_release_with_backlog_ok（追赶期放行）双路径覆盖。
- test_ws_acceptance.TestResolveAcceptance.test_inbuilt_lcview_liveness_present 既有失败（clean HEAD 5cdf0dc 即红,系 HAL 退役后 lcview-liveness 用例变更但测试期望未同步更新）,与本次批次无关,未纳入改动。
