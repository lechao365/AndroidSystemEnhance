- schema_version: 1
- batch_id: be5969c4fa93
- batch_base: f887fc88be2c
- verified_commit: f887fc88be2c
- verify_mode: board
- result: pass
- build: pass
- push_board: pass
- acceptance: {"overall":"pass","items":[{"tag":"svc:lechao_lciod_hal","status":"pass","detail":"init.svc.lechao_lciod_hal=running exit=0"},{"tag":"svc:lechao_lciod","status":"pass","detail":"init.svc.lechao_lciod=running exit=0"},{"tag":"log:\"monitor: minor=\"","status":"pass","detail":"logcat 命中关键字 monitor: minor=（取回 441516 字符）"},{"tag":"boot","status":"pass","detail":"sys.boot_completed=1"}]}
- elapsed_s: 0
- summary: 修 lciod 三处假绿与速率溢出：空expect判红+error_count/event_drop_count==0断言+delta先跑stats校验+速率中间量__uint128_t，上板 lciod-liveness 4/4 全过

## body

## CDP 原文
-sv base:f887fc88be2c
意图: 修 lciod 三处假绿与速率溢出
验收: svc:lechao_lciod_hal svc:lechao_lciod boot
方向: 1) lciod_check.py L224 的 --expect 默认空,delta 时 L191 循环不执行故直接过,yaml 漏写即核心增量断言全跳过;空 expect 判红并补单测。2) 同文件 L44 与 L49 收 error_count 与 event_drop_count 却只校验存在与非负,无符号非负恒真,累计错误或丢事件仍判绿;加等于 0 断言。3) validate_devices 只在 L235 与 L239 被调,delta 分支不调,enabled 等于 0 与缺字段与 abi 漂移在 trigger 末步被绕过;delta 取数后先跑该校验。4) service.cpp L55 与 L61 以 uint64 乘 1e9,累计约 17GiB 与 9GiB 即回绕致速率失真;中间量改 __uint128_t 并补溢出单测。

## 编辑（方向 1-4）
- 方向 1（空 expect 判红）: cases/lciod_check.py diff_devices 开头对空 expect_fields 判红（"delta 模式未指定 --expect 字段（核心增量断言全跳过）"），并补单测 DiffDevicesTest.test_empty_expect_fields_is_error
- 方向 2（error_count/event_drop_count ==0 断言）: cases/lciod_check.py validate_devices 对两字段加等于 0 断言（无符号计数非负恒真，累计错误/丢事件不得全绿），并补单测 test_error_count_nonzero_is_error / test_event_drop_count_nonzero_is_error
- 方向 3（delta 先跑 stats 校验）: main() delta 分支取数后先 validate_devices(devices)，enabled=0/缺字段/abi 漂移不再被 trigger 末步绕过
- 方向 4（__uint128_t 中间量）: daemon/service.cpp ComputeAverageRate（total*1e9）与 ComputeKbRate（bytes*1e9）中间量改 __uint128_t，消除约 17GiB 累计回绕致速率失真；Service_test.cpp 补 ComputeAverageRateTest.LargeBytes_NoOverflow（read=write=1e10,2s→1e10 B/s）与 ComputeKbRateTest.LargeBytes_NoOverflow（18.6GiB/1s→19531250 KB/s）

## 单测
- harness lciod_check pytest：29 全过（新增 3 项：空 expect / error_count 非 0 / event_drop_count 非 0）
- AOSP lechao_lciod_unit_test + lechao_lciod_hal_test：编译通过（含新增溢出单测，__uint128_t 编译 OK）

## 编译
- make lechao_lciod_unit_test lechao_lciod_hal_test：成功（02:57）
- make lechao_lciod lechao_lciod_hal vendor.lechao.lciod.IIoHal-vintf lciod_probe：成功（7s）

## 推送与上板
- adb 推送 6 产物（daemon/hal/init/vintf/probe）到 /system 与 /vendor；reboot 生效
- 验收 lciod-liveness 4/4 全过（首跑 log 判据 miss 系 reboot 后时序：monitor 首条日志在服务启动约 10s 后打出，验收过早执行；monitor 为 10s 周期持续性日志，等待后重跑命中）

## 验收逐项（lciod-liveness → pass 4/4）
- svc:lechao_lciod_hal → pass（init.svc.lechao_lciod_hal='running'）
- svc:lechao_lciod → pass（init.svc.lechao_lciod='running'）
- log:"monitor: minor=" → pass（命中，monitor: minor=0 read_rate=505 KB/s 持续输出）
- boot → pass（sys.boot_completed='1'）
