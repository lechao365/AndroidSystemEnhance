# ============================================================================
# paths.conf — harness 路径配置（单一事实源）
# 相对路径基于项目根解析；KERNEL_WS/AOSP_WS 支持环境变量覆盖（${VAR:-} 语法）
# ============================================================================

PATCHS_DIR="code/rpi5"
KERNEL_WS="${KERNEL_WS:-}"
AOSP_WS="${AOSP_WS:-}"
